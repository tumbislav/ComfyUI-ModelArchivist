# ---------------------------------------------------------------------------
# system: ModelArchivist
# file: config.toml
# purpose: Application config
# ---------------------------------------------------------------------------

[paths]
comfy = "{$app}/test/data/ComfyUI"
archive = "{$app}/test/data/archive"
user = "{$app}/test/data"
database = "{$app}/test/model_archivist.db"

[models]
active = "{$comfy}/models"
archive = "{$archive}/models"
extensions = [".safetensors", ".sft", ".ckpt", ".pt", ".pth", ".gguf"]
ignore = ["examples", "configs"]

[models.types]
checkpoints = "Checkpoint"
clip = "CLIP"
clip_vision = "CLIP Vision"
controlnet = "Controlnet"
diffusers = "Diffuser"
diffusion_models = "Diffusion model"
embeddings = "Embedding"
gligen = "GLIGEN"
hypernetworks = "Hypernetwork"
latent_upscale_models = "Upscaler (latent)"
loras = "LoRA"
model_patches = "Model patch"
photomaker = "PhotoMaker"
style_models = "Style"
text_encoders = "Text encoder"
unet = "U-Net"
upscale_models = "Upscaler"
vae = "VAE"
vae_approx = "Tiny AutoEncoder"

[[models.extras]]
yaml = "{$comfy}/extra_models_1.yaml"
archive = "{$archive}/extra_1"

[workflows]

[[workflows.folders]]
active = "{$user}/workflows"
archive = "{$archive}/workflows"

[web]
base_url = "http://127.0.0.1"
port = 5173

[options]
update_json_metadata = true     # update metadata when values change - has consequences for Lora
ignore_unknown_types = false    # ignore models not in the model_types list
